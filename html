<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dem OS - Browser-based macOS Experience</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #2c3e50 0%, #1a1a2e 100%);
            height: 100vh;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #fff;
        }
        
        .desktop {
            width: 95vw;
            height: 95vh;
            background: url('https://images.unsplash.com/photo-1518837695005-2083093ee35b?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2340&q=80') no-repeat center center;
            background-size: cover;
            border-radius: 14px;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.5);
            position: relative;
            display: flex;
            flex-direction: column;
        }
        
        .menu-bar {
            height: 28px;
            background: rgba(50, 50, 50, 0.8);
            backdrop-filter: blur(10px);
            display: flex;
            align-items: center;
            padding: 0 20px;
            font-size: 14px;
            color: #f0f0f0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .apple-logo {
            margin-right: 15px;
            font-size: 18px;
        }
        
        .menu-item {
            margin-right: 15px;
            cursor: pointer;
            padding: 4px 8px;
            border-radius: 4px;
            transition: background 0.2s;
        }
        
        .menu-item:hover {
            background: rgba(255, 255, 255, 0.1);
        }
        
        .menu-right {
            margin-left: auto;
            display: flex;
            align-items: center;
        }
        
        .menu-right i {
            margin-left: 15px;
            font-size: 14px;
        }
        
        .desktop-content {
            flex: 1;
            padding: 20px;
            position: relative;
        }
        
        .desktop-icon {
            position: absolute;
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 80px;
            cursor: pointer;
            transition: all 0.2s;
        }
        
        .desktop-icon:hover {
            transform: scale(1.05);
        }
        
        .desktop-icon img {
            width: 50px;
            height: 50px;
            object-fit: contain;
            margin-bottom: 8px;
        }
        
        .desktop-icon span {
            color: white;
            text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
            font-size: 13px;
            text-align: center;
        }
        
        .folder {
            top: 40px;
            left: 40px;
        }
        
        .github {
            top: 40px;
            left: 140px;
        }
        
        .terminal {
            top: 150px;
            left: 40px;
        }
        
        .settings {
            top: 150px;
            left: 140px;
        }
        
        .window {
            position: absolute;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            min-width: 500px;
            min-height: 350px;
            display: none;
            flex-direction: column;
            backdrop-filter: blur(10px);
        }
        
        .window-header {
            height: 38px;
            background: rgba(230, 230, 230, 0.9);
            display: flex;
            align-items: center;
            padding: 0 15px;
            justify-content: space-between;
            border-bottom: 1px solid #d0d0d0;
            cursor: move;
        }
        
        .window-controls {
            display: flex;
        }
        
        .control {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            margin-right: 8px;
            cursor: pointer;
        }
        
        .close { background: #ff5f56; }
        .minimize { background: #ffbd2e; }
        .maximize { background: #27c93f; }
        
        .window-title {
            font-size: 14px;
            color: #555;
            font-weight: 500;
        }
        
        .window-body {
            flex: 1;
            padding: 20px;
            color: #333;
            overflow: auto;
        }
        
        .dock {
            position: absolute;
            bottom: 10px;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(80, 80, 80, 0.4);
            backdrop-filter: blur(20px);
            height: 60px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            padding: 0 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .dock-item {
            width: 50px;
            height: 50px;
            margin: 0 5px;
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.9);
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            transition: all 0.2s;
            font-size: 22px;
            color: #444;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        }
        
        .dock-item:hover {
            transform: scale(1.2);
            margin: 0 15px;
        }
        
        .finder { background: #007aff; color: white; }
        .github-dock { background: #333; color: white; }
        .terminal-dock { background: #333; color: #7cfc00; }
        .settings-dock { background: #555; color: white; }
        .trash { background: linear-gradient(45deg, #ff5f56, #ff9950); color: white; }
        
        .active {
            transform: scale(1.15);
            margin: 0 10px;
        }
        
        .footer {
            text-align: center;
            padding: 15px;
            color: rgba(255, 255, 255, 0.7);
            font-size: 14px;
        }
        
        .footer a {
            color: #64b5f6;
            text-decoration: none;
        }
        
        .code {
            background: #2d2d2d;
            color: #f8f8f2;
            padding: 15px;
            border-radius: 6px;
            font-family: 'Monaco', 'Menlo', monospace;
            font-size: 13px;
            overflow-x: auto;
            margin: 15px 0;
        }
        
        .terminal-text {
            color: #7cfc00;
            margin-bottom: 10px;
        }
        
        .prompt { color: #64b5f6; }
    </style>
</head>
<body>
    <div class="desktop">
        <div class="menu-bar">
            <div class="apple-logo">􀣺</div>
            <div class="menu-item">Dem OS</div>
            <div class="menu-item">File</div>
            <div class="menu-item">Edit</div>
            <div class="menu-item">View</div>
            <div class="menu-item">Window</div>
            <div class="menu-item">Help</div>
            
            <div class="menu-right">
                <i class="fas fa-wifi"></i>
                <i class="fas fa-battery-three-quarters"></i>
                <i class="fas fa-search"></i>
                <span>Sun 2:15 PM</span>
            </div>
        </div>
        
        <div class="desktop-content">
            <div class="desktop-icon folder">
                <img src="https://img.icons8.com/fluency/96/000000/folder-invoices.png" alt="Folder">
                <span>Documents</span>
            </div>
            
            <div class="desktop-icon github">
                <img src="https://img.icons8.com/ios-glyphs/90/000000/github.png" alt="GitHub">
                <span>GitHub</span>
            </div>
            
            <div class="desktop-icon terminal">
                <img src="https://img.icons8.com/ios-filled/100/000000/console.png" alt="Terminal">
                <span>Terminal</span>
            </div>
            
            <div class="desktop-icon settings">
                <img src="https://img.icons8.com/ios-filled/100/000000/settings.png" alt="Settings">
                <span>Settings</span>
            </div>
            
            <!-- GitHub Window -->
            <div class="window" id="github-window">
                <div class="window-header">
                    <div class="window-controls">
                        <div class="control close" onclick="closeWindow('github-window')"></div>
                        <div class="control minimize" onclick="minimizeWindow('github-window')"></div>
                        <div class="control maximize"></div>
                    </div>
                    <div class="window-title">GitHub - Dem OS Repository</div>
                    <div></div>
                </div>
                <div class="window-body">
                    <h2>Dem OS Project</h2>
                    <p>A browser-based macOS experience designed for GitHub.</p>
                    
                    <div class="code">
                        <span class="prompt">$ </span>git clone https://github.com/username/dem-os.git<br>
                        <span class="prompt">$ </span>cd dem-os<br>
                        <span class="prompt">$ </span>npm install<br>
                        <span class="prompt">$ </span>npm start
                    </div>
                    
                    <h3>Project Details</h3>
                    <ul>
                        <li>Built with HTML5, CSS3, and JavaScript</li>
                        <li>Responsive design that works on all modern browsers</li>
                        <li>macOS-style UI components</li>
                        <li>Interactive desktop environment</li>
                    </ul>
                </div>
            </div>
            
            <!-- Terminal Window -->
            <div class="window" id="terminal-window">
                <div class="window-header">
                    <div class="window-controls">
                        <div class="control close" onclick="closeWindow('terminal-window')"></div>
                        <div class="control minimize" onclick="minimizeWindow('terminal-window')"></div>
                        <div class="control maximize"></div>
                    </div>
                    <div class="window-title">Terminal - zsh</div>
                    <div></div>
                </div>
                <div class="window-body" style="background: #1d1f21; color: #c5c8c6;">
                    <div class="terminal-text">Last login: Sun Jun 12 14:15:28 on ttys002</div>
                    <div class="terminal-text"><span class="prompt">dem-os@github % </span>ls -la</div>
                    <div class="terminal-text">total 16<br>
                    drwxr-xr-x   6 dem-os  staff   192 Jun 12 14:15 .<br>
                    drwxr-xr-x+ 60 dem-os  staff  1920 Jun 12 14:15 ..<br>
                    -rw-r--r--   1 dem-os  staff   284 Jun 12 14:15 README.md<br>
                    drwxr-xr-x   3 dem-os  staff    96 Jun 12 14:15 src<br>
                    drwxr-xr-x   4 dem-os  staff   128 Jun 12 14: public</div>
                    <div class="terminal-text"><span class="prompt">dem-os@github % </span><span class="cursor">█</span></div>
                </div>
            </div>
        </div>
        
        <div class="dock">
            <div class="dock-item finder active" onclick="openWindow('finder-window')">
                <i class="fas fa-desktop"></i>
            </div>
            <div class="dock-item github-dock" onclick="openWindow('github-window')">
                <i class="fab fa-github"></i>
            </div>
            <div class="dock-item terminal-dock" onclick="openWindow('terminal-window')">
                <i class="fas fa-terminal"></i>
            </div>
            <div class="dock-item settings-dock">
                <i class="fas fa-cog"></i>
            </div>
            <div class="dock-item trash">
                <i class="fas fa-trash"></i>
            </div>
        </div>
    </div>
    
    <div class="footer">
        Dem OS - Browser-based macOS experience | Created for GitHub | <a href="https://github.com">View on GitHub</a>
    </div>

    <script>
        // Function to open windows
        function openWindow(windowId) {
            // Close all windows first
            document.querySelectorAll('.window').forEach(window => {
                window.style.display = 'none';
            });
            
            // Open the requested window
            const window = document.getElementById(windowId);
            if (window) {
                window.style.display = 'flex';
                // Position the window in the center
                window.style.top = '50%';
                window.style.left = '50%';
                window.style.transform = 'translate(-50%, -50%)';
            }
            
            // Update active dock item
            document.querySelectorAll('.dock-item').forEach(item => {
                item.classList.remove('active');
            });
            event.currentTarget.classList.add('active');
        }
        
        // Function to close windows
        function closeWindow(windowId) {
            const window = document.getElementById(windowId);
            if (window) {
                window.style.display = 'none';
            }
        }
        
        // Function to minimize windows (simplified)
        function minimizeWindow(windowId) {
            const window = document.getElementById(windowId);
            if (window) {
                window.style.display = 'none';
            }
        }
        
        // Make windows draggable
        let isDragging = false;
        let currentWindow = null;
        let offsetX, offsetY;
        
        document.querySelectorAll('.window-header').forEach(header => {
            header.addEventListener('mousedown', function(e) {
                isDragging = true;
                currentWindow = this.parentElement;
                offsetX = e.clientX - currentWindow.getBoundingClientRect().left;
                offsetY = e.clientY - currentWindow.getBoundingClientRect().top;
                currentWindow.style.cursor = 'grabbing';
            });
        });
        
        document.addEventListener('mousemove', function(e) {
            if (isDragging && currentWindow) {
                currentWindow.style.left = (e.clientX - offsetX) + 'px';
                currentWindow.style.top = (e.clientY - offsetY) + 'px';
                currentWindow.style.transform = 'none';
            }
        });
        
        document.addEventListener('mouseup', function() {
            isDragging = false;
            if (currentWindow) {
                currentWindow.style.cursor = 'default';
                currentWindow = null;
            }
        });
        
        // Open GitHub window by default
        window.onload = function() {
            openWindow('github-window');
        };
    </script>
</body>
</html>
